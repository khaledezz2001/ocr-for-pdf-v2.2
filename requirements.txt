# RunPod serverless
runpod

# Image / PDF processing
pillow
pdf2image
numpy<2.0

# Chandra OCR package — provides prompts, output parser (parse_markdown)
# No [hf] extra needed — vLLM handles inference, transformers only for processor
chandra-ocr
transformers

# HF hub for model download at build time
huggingface_hub
protobuf
